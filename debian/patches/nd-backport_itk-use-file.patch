--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -58,6 +58,7 @@ else()
     #Check Qt4 Library [Qt4] (4.4.0)
     set(QT_USE_IMPORTED_TARGETS TRUE)
     find_package(Qt4 4.4.0 REQUIRED)
+    include(${QT_USE_FILE})
 
 endif()
 
